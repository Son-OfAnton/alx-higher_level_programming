#!/usr/bin/python3


def weight_average(my_list=[]):
    result = 0
    b = 0
    if len(my_list) == 0:
        return total
    for i in my_list:
        result += (i[0] * i[1])
        b += x[1]
    return (result / b)
